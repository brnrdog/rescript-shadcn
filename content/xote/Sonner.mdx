---
title: Sonner
description: An opinionated toast component for React.
base: xote
component: true
links:
  doc: https://xote.dev/
  api: https://github.com/brnrdog/xote#readme
---
<ComponentPreview name="SonnerDemo" />

## About

Sonner is built and maintained by [emilkowalski](https://twitter.com/emilkowalski).

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<Steps className="mb-0 pt-2">

<Step>Run the following command:</Step>

```bash
npx shadcn@latest add @rescript-shadcn/Sonner
```

<Step>Add the Toaster component</Step>

```res title="app/layout.res" {1,9} showLineNumbers
module RootLayout = {
  @react.component 
  let make = (~children) => 
    <html lang="en">
      <head />
      <body>
        <main>{children}</main>
        <Sonner />
      </body>
    </html>
}
```

</Steps>

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash
npm install xote
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  name="Sonner"
  title="components/ui/Sonner.res"
/>

<Step>Add the Toaster component</Step>

```res showLineNumbers title="app/layout.res" {1,8}
module RootLayout = {
  @react.component 
  let make = (~children) => 
    <html lang="en">
      <head />
      <body>
        <Toaster />
        <main>{children}</main>
      </body>
    </html>
}
```

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```res showLineNumbers
Sonner.toast("Event has been created")
```

## API Reference

This component is plain markup built with [xote](https://xote.dev/) —
everything it renders lives in the file you copied, and there is no runtime
dependency beyond xote itself.
