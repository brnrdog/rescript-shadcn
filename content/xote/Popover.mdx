---
title: Popover
description: Displays rich content in a portal, triggered by a button.
base: xote
component: true
links:
  doc: https://xote.dev/
  api: https://github.com/brnrdog/xote#readme
---

<ComponentPreview name="PopoverDemo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @rescript-shadcn/Popover
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash
npm install xote rescript-xote-base
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  name="Popover"
  title="components/ui/Popover.res"
/>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```res
<Popover>
  <Popover.Trigger> {"Open"} </Popover.Trigger>
  <Popover.Content>
    <Popover.Title> {"Dimensions"} </Popover.Title>
    <Popover.Description> {"Set the dimensions for the layer."} </Popover.Description>
  </Popover.Content>
</Popover>
```

## Positioning

The content is portaled to the end of `document.body` and positioned against
the trigger, flipping to the opposite side when the preferred one has no room.
Use `side` and `align` to choose the preferred placement, and `sideOffset` /
`alignOffset` to nudge it.

```res
<Popover.Content side=Right align=Start sideOffset=8. />
```

## API Reference

Behaviour, ARIA wiring and keyboard interaction come from [`rescript-xote-base`](https://github.com/miriadhq/rescript-shadcn/tree/main/packages/xote-base); the component in your project owns the markup and the classes.
